meta {
  name: -John- Add Another Playlist with Valid Payload
  type: http
  seq: 4
}

post {
  url: http://localhost:{{port}}/playlists
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessTokenJohn}}
}

body:json {
  {
      "name": "Lagu Untuk Membaca"
  }
}

tests {
  test('it should response 201 status code', () => {
  //     expect(pm.response).to.have.status(201);
  });
  
  test('response Content-Type header should have application/json value', () => {
  //     expect(pm.response.headers.get('Content-Type')).to.includes('application/json');
  }); 
  
  test('response body should an object', () => {
      const responseJson = res.getBody();
      expect(responseJson).to.be.an('object');
  });
  
  test('response body have correct property and value', () => {
      const responseJson = res.getBody();
  
      expect(responseJson.status).to.equal('success');
      expect(responseJson.data).to.be.a('object');
      expect(responseJson.data.playlistId).to.be.a('string');
  
      bru.setEnvVar('playlistIdJohn', responseJson.data.playlistId);
  });
}
