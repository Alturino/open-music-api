meta {
  name: Add Album With No Songs
  type: http
  seq: 2
}

post {
  url: http://localhost:{{port}}/albums
  body: json
  auth: none
}

body:json {
  {
      "name": "Viva la vida",
      "year": 2008
  }
}

tests {
  const responseJson = res.getBody();
  bru.setEnvVar('albumIdWithNoSongs', responseJson.data.albumId);
}
