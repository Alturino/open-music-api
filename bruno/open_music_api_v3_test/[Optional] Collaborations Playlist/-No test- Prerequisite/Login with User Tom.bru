meta {
  name: Login with User Tom
  type: http
  seq: 6
}

post {
  url: http://localhost:{{port}}/authentications
  body: json
  auth: none
}

body:json {
  {
      "username": "{{usernameTom}}",
      "password": "secret"
  }
}

tests {
  const { data: { accessToken } } = res.getBody();
  bru.setEnvVar('accessTokenTom', accessToken);
}
